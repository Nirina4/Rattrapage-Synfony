<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Gestion de Projet - Accueil</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background: #f4f6f8;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 500px;
            margin: 60px auto;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.08);
            padding: 40px 32px 32px 32px;
            text-align: center;
        }
        h2 {
            margin-bottom: 8px;
            color: #2d3a4b;
            font-weight: 700;
        }
        p {
            color: #6c7a89;
            margin-bottom: 32px;
        }
        .btn-group {
            display: flex;
            flex-direction: column;
            gap: 18px;
        }
        .btn {
            display: block;
            padding: 14px 0;
            border-radius: 6px;
            background: #2563eb;
            color: #fff;
            font-size: 1.1rem;
            font-weight: 500;
            text-decoration: none;
            transition: background 0.2s;
            box-shadow: 0 2px 8px rgba(37,99,235,0.08);
        }
        .btn:hover {
            background: #1741a6;
        }
        @media (max-width: 600px) {
            .container {
                padding: 24px 8px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Gestion de Projet</h2>
        <p>Accédez rapidement aux modules principaux de votre application :</p>
        <div class="btn-group">
            <a href="{{ path('app_user_index') }}" class="btn">Utilisateurs</a>
            <a href="/mission/" class="btn">Missions</a>
            <a href="{{ path('app_task_index') }}" class="btn">Tâches</a>
        </div>
    </div>
</body>
</html> 